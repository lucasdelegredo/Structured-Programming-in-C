#include <stdio.h>
#include <stdlib.h>

int somar_inteiros(int n1, int n2) {
    // incluir aqui a sua solução
    int soma;
    soma = n1 + n2;
    return(soma);
}

void main()
{
    float n1;
    float n2;
    
    scanf("%f",&n1);
    scanf("%f",&n2);
    printf("%f",operacao(n1,n2,simb));
}